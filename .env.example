# Local development API key for Vite (only for local testing)
# WARNING: Do NOT commit a real production API key to a public repo.
# For Vite, variables exposed to client-side code must start with VITE_.
VITE_API_KEY=geminikey
