# Local development API key (server-side). Do NOT expose client-side keys.
# WARNING: Do NOT commit a real production API key to a public repo.
# Place your server API key in `.env.local` as `GEMINI_API_KEY`.
GEMINI_API_KEY=geminikey
GEMINI_MODEL=gemini-2.5-flash-lite
